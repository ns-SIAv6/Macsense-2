# SYSTEM_AUDIT

## Repo: ns-SIAv6/Macsense-2

Short system brief:
- Android application (Gradle/Kotlin).
- Build system: Gradle (Kotlin DSL).
- Runtime: Android.
- Notable files: build.gradle.kts, settings.gradle.kts, gradle.properties, app/ module.
- Contains an upload keystore file: my-upload-key.jks (sensitive). .env.example exists.

Unknowns / blockers:
- No CI/workflows present.
- No test framework configuration observed in root.
- Needs secret removal from git history if removed.

